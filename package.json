{
  "name": "lola-tseng-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Markdown-driven portfolio website.",
  "scripts": {
    "build": "node scripts/build.js",
    "watch": "node scripts/build.js --watch",
    "dev": "node scripts/build.js --watch --serve"
  },
  "engines": {
    "node": ">=18"
  }
}
